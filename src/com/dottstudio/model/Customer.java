package com.dottstudio.model;

public class Customer extends User {
    // Customer is essentially a User with a specific Role.
    // We keep this class for type safety and potential future extension.

    public Customer() {
        super();
    }
}
